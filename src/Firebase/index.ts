import FirebaseContext, { withFirebase } from './FirebaseContext';
import Firebase, { FirebaseInterface } from './Firebase';

export default Firebase;

export { FirebaseContext, withFirebase };
export type { FirebaseInterface };
